票务管理系统演示,顺便求职

第一部分,基础结构及非业务相关:
1.权限模块
2.角色模块
3.账号模块

第二部分,销售点业务以及票务相关:
4.销售点管理模块
	新增销售点
	删除销售点
	更新销售点
5.销售点账户管理模块
	新增销售点账户(该账户为新增销售点自动匹配一个账户)
	删除销售点账户
	更新销售点账户
6.票务管理模块
	年票入库(新增年票)
		使用服务器时间为标准,传到前端,在页面上展示(前端的时间只作为展示用途,后台重新设置入库时间)
		//region 新增年票记录Logic
			//1.检查票号是否合法:使用包装类的compareTo 方法 源码:public static int compare(int x, int y) {
															return (x < y) ? -1 : ((x == y) ? 0 : 1);
														}
			//2.判断当前票号和已存在的票号是否重复,如果重复打回
			//3.设置新增时间
			//4.每次插入都要设置更新数量
			//5.将每次入库的备注设置为票号区间
			//6.保存日志
			//7.将年票保存到年票表中,每一张年票都要保存一条
			//8.事务回滚
        //endregion
	年票下发
	年票作废
7.景区管理模块




